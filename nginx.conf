server {
    listen 80;
    server_name alfa-pay.org www.alfa-pay.org manager.alfa-pay.org;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name alfa-pay.org www.alfa-pay.org;

    ssl_certificate /etc/letsencrypt/live/alfa-pay.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/alfa-pay.org/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location /api/ {
       proxy_pass http://172.17.0.1:5000;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_redirect off;
    }

    location / {
       proxy_pass http://172.17.0.1:3000;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_redirect off;
    }
}

server {
    listen 443 ssl;
    server_name manager.alfa-pay.org;

    ssl_certificate /etc/letsencrypt/live/alfa-pay.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/alfa-pay.org/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://172.17.0.1:5173; # Измени на нужный backend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
}